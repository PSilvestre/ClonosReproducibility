CONTEXT = [
  NETWORK = "YES",
  SSH_PUBLIC_KEY = "<$ssh_key>",
  START_SCRIPT = "sudo mkfs.ext4 /dev/vdb && sudo mkdir -p /media/extra && sudo mount /dev/vdb /media/extra && sudo mkdir -p /media/extra/{0..200} && sudo chmod -R 777 /media/extra  && sudo /home/ubuntu/join_cluster.sh <$cluster_name> ",
  USERNAME = "ubuntu" ]
CPU = "<$follower_cpu>"
DISK = [
  IMAGE = "KubernetesBase",
  IMAGE_UNAME = "delta",
	SIZE = 36000]
DISK = [
  FORMAT = "raw",
  SIZE = "<$follower_disk>",
  TYPE = "fs" ]
FEATURES = [
  ACPI = "yes",
  LOCALTIME = "no" ]
INPUT = [
  BUS = "usb",
  TYPE = "tablet" ]
LOGO = "images/logos/ubuntu.png"
MEMORY = "<$follower_memory>"
NIC = [
  NETWORK = "internet",
  NETWORK_UNAME = "oneadmin" ]
NIC = [
  NETWORK = "camparatia-tudelft.int",
  NETWORK_UNAME = "camparatia-asteriosk" ]
OS = [
  ARCH = "x86_64" ]
VCPU = "<$follower_cpu>"
